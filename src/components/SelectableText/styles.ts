const container: React.CSSProperties = {
  position: "relative",
};
const highlightStyle: React.CSSProperties = {
  display: "inline",
  cursor: "pointer",
};

const selectedStyle: React.CSSProperties = {
  background: "#6C72F6",
  color: "#FFF",
};

export default { container, highlightStyle, selectedStyle };
